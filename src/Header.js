import React from 'react';
import './Header.css'; // Import CSS for styling

const Header = () => {
  return (
    <header className="app-header">
      <h1 className="app-title">KOL Ranking dashboard</h1>
    </header>
  );
};

export default Header;
